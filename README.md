# Bear running app
This app with simple session auth, allows you to create jogs history with ability to change, delete if necessary; filter them by date.

# Installation
1. Clone repo
2. run "npm install"
3. run "npm run dev" for starting app.
4. run "npm test" for running tests.
